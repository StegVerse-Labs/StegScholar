# OTF ICRP package validation request

Validate the complete OTF ICRP support package, dedicated validator, workflow integration, task registry, and sub-handoff.

Expected result: both funding validators complete successfully and upload both JSON receipts in one artifact.

The validation marker must not be merged after evidence is recorded.
