# Independent Review Predicate Validation Plan

Validate `INDEPENDENT-REVIEW-PREDICATE-001` by requiring:

1. deterministic acceptance when all five separation dimensions are evidenced `SEPARATE`;
2. `NOT_INDEPENDENT` for any unresolved disqualifying conflict across common control, rule authorship/control, material financial interest, reviewer/original-evaluator identity, or execution-path control;
3. `UNRESOLVED` when review is unavailable or any required separation evidence is missing;
4. explicit preservation of `review_available != independent_review_available`;
5. `authority_effect: NONE` and no ALLOW/DENY semantics;
6. no mutation of the completed gate-legitimacy formalism or existing Governable Autonomy review schema in this task.

Source completion alone is insufficient; exact-head validation and merge evidence are required before canonical completion is claimed.
